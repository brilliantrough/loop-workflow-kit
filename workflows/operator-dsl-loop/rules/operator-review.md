# Review Rules

- Route back to `plan` when the failure is strategy-level rather than a local implementation bug.
- Route to `finalize` only when correctness and performance evidence are both credible.
- Record machine-readable review output before any publish or registration step.
