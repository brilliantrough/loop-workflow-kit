# __WORKFLOW_NAME__

Prototype workflow scaffold generated by `loop-workflow-kit`.

## Startup

```bash
bun run prototype:run -- --workflow __WORKFLOW_PATH__
```

## Replay

```bash
bun run prototype:replay -- --workflow __WORKFLOW_PATH__ --mode inspect --run-dir __SAMPLE_RUN_DIRECTORY__
bun run prototype:replay -- --workflow __WORKFLOW_PATH__ --mode sessions --run-dir __SAMPLE_RUN_DIRECTORY__
```

## What To Adapt

- Rewrite `prompts/` to match the real workflow roles.
- Replace `checks/fake-*.ts` with real validators.
- Replace `reference/` and `rules/` with domain-specific inputs.
- Update `artifacts/input.json` and `input.schema.json` to the target repository contract.
- Keep the workflow graph, artifact contracts, and replay surfaces stable while adapting the runtime.
